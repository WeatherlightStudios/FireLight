#include "Spawner.h"
#include "Draggable.h"

Spawner::Spawner(Scene* scene, std::shared_ptr<GameObject> gridManager) {
	Spawner::scene = scene;
	Spawner::gridManager = gridManager;
}

void Spawner::Init() {
	
}

void Spawner::Update() {

}
